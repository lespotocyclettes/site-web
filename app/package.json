{
	"name": "@lespotocyclettes/app",
	"version": "0.1.0",
	"private": true,
	"description": "Mini-app de gestion des vélos : fondation CSS et styleguide.",
	"license": "GPL-2.0-or-later",
	"scripts": {
		"lint:css": "stylelint \"src/**/*.css\"",
		"typecheck": "tsc --noEmit",
		"check:contrast": "node scripts/check-contrast.mjs",
		"check:tokens": "node scripts/check-tokens.mjs",
		"verify": "npm run lint:css && npm run typecheck && npm run check:tokens && npm run check:contrast"
	},
	"devDependencies": {
		"@elecdeer/stylelint-rscss": "^2.0.0",
		"@types/react": "^19.3.0",
		"stylelint": "^17.0.0",
		"typescript": "^7.0.2"
	},
	"dependencies": {
		"react": "^19.3.0"
	}
}
